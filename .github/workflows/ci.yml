# Pipeline único – toda a lógica fica aqui (como o include do GitLab).
# O projeto só chama este workflow; quem decide o que rodar é o evento (push/PR/tag).
name: CI

on:
  workflow_call:
    inputs:
      python-version:
        required: false
        default: '3.11'
        type: string
      image-name:
        description: 'Docker image name (e.g. myorg/myapp)'
        required: true
        type: string
      dockerfile-path:
        required: false
        default: './Dockerfile'
        type: string
      coverage-path:
        required: false
        default: 'coverage.xml'
        type: string
      main-branch:
        required: false
        default: 'main'
        type: string
      ci-platform-repo:   # 👈 ISSO PRECISA EXISTIR
        required: false
        default: 'TourinhoM/org-ci-platform'
        type: string
    secrets:
      DOCKER_USERNAME:
        required: false
      DOCKER_PASSWORD:
        required: false
      SONAR_TOKEN:
        required: false
        
permissions:
  contents: read
  security-events: write
  actions: read
  packages: write
  
jobs:
  # ---- Push / PR: Trivy FS Scan ----
  trivy-fs:
    name: Trivy FS Scan
    runs-on: ubuntu-latest
    if: github.event_name == 'push' || github.event_name == 'pull_request'
    steps:
      - uses: actions/checkout@v4
      - uses: TourinhoM/org-ci-platform/.github/actions/trivy-fs@main

  # ---- Unit Test ----
  unit-test:
    name: Unit tests (Python)
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request' || (github.event_name == 'push' && !startsWith(github.ref, 'refs/tags/') && github.ref != format('refs/heads/{0}', inputs.main-branch))
    needs: [trivy-fs]
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: ${{ github.event_name == 'pull_request' && 0 || 1 }}
      - uses: TourinhoM/org-ci-platform/.github/actions/unit-test-python@main
        with:
          python-version: ${{ inputs.python-version }}
          coverage-path: ${{ inputs.coverage-path }}

  # ---- SonarQube ----
  sonarqube:
    name: SonarQube
    runs-on: ubuntu-latest
    needs: [unit-test]
    if: github.event_name == 'pull_request' || (github.event_name == 'push' && github.ref == format('refs/heads/{0}', inputs.main-branch))
    env:
      SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
    steps:
      - uses: actions/checkout@v4
      - uses: TourinhoM/org-ci-platform/.github/actions/sonarqube@main
        with:
          coverage-path: ${{ inputs.coverage-path }}
          sonar-args: >-
            ${{ github.event_name == 'pull_request' && format('-Dsonar.pullrequest.key={0} -Dsonar.pullrequest.branch={1} -Dsonar.pullrequest.base={2}', github.event.pull_request.number, github.head_ref, inputs.main-branch) || '' }}

  # ---- Docker build branch ----
  docker-build-branch:
    name: Docker build (branch - SHA)
    runs-on: ubuntu-latest
    needs: [trivy-fs, unit-test]
    if: github.event_name == 'push' && !startsWith(github.ref, 'refs/tags/') && github.ref != format('refs/heads/{0}', inputs.main-branch)
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4
      - uses: TourinhoM/org-ci-platform/.github/actions/docker-build-push@main
        with:
          dockerfile-path: ${{ inputs.dockerfile-path }}
          tags: |
            ghcr.io/${{ github.repository }}:${{ github.sha }}

  # ---- Docker build release ----
  docker-build-release:
    name: Docker build (release)
    runs-on: ubuntu-latest
    needs: [unit-test]
    if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags/')
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4
      - uses: TourinhoM/org-ci-platform/.github/actions/docker-build-push@main
        with:
          dockerfile-path: ${{ inputs.dockerfile-path }}
          tags: |
            ghcr.io/${{ github.repository }}:${{ github.ref_name }}
            ghcr.io/${{ github.repository }}:latest

  # ---- Trivy scan branch image ----
  trivy-image-branch:
    name: Trivy scan (branch image)
    runs-on: ubuntu-latest
    needs: [docker-build-branch]
    if: github.event_name == 'push' && !startsWith(github.ref, 'refs/tags/') && github.ref != format('refs/heads/{0}', inputs.main-branch)
    permissions:
      contents: read
      packages: read
    steps:
      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ github.token }}
      - uses: TourinhoM/org-ci-platform/.github/actions/trivy-image@main
        with:
          image-ref: ghcr.io/${{ github.repository }}:${{ github.sha }}

  # ---- Trivy scan release image ----
  trivy-image-release:
    name: Trivy scan (release image)
    runs-on: ubuntu-latest
    needs: [docker-build-release]
    if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags/')
    permissions:
      contents: read
      packages: read
    steps:
      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ github.token }}
      - uses: TourinhoM/org-ci-platform/.github/actions/trivy-image@main
        with:
          image-ref: ghcr.io/${{ github.repository }}:${{ github.ref_name }}

  # ---- Semantic commit check ----
  semantic-commit-check:
    name: Semantic commit check
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request'
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: TourinhoM/org-ci-platform/.github/actions/semantic-commit-check@main

  # ---- Semantic release ----
  semantic-release:
    name: Semantic Release
    runs-on: ubuntu-latest
    needs: [sonarqube]
    if: github.event_name == 'push' && github.ref == format('refs/heads/{0}', inputs.main-branch)
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          persist-credentials: false
      - name: Semantic Release
        uses: cycjimmy/semantic-release-action@v4
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          extra_plugins: |
            @semantic-release/changelog
            @semantic-release/git
          branch: ${{ inputs.main-branch }}