# Reusable: Deploy to Railway (placeholder – implement when ready)
name: Deploy Railway

on:
  workflow_call:
    inputs:
      railway-service:
        required: false
        type: string
      environment:
        required: false
        default: 'production'
        type: string
    secrets:
      RAILWAY_TOKEN:
        required: false
        description: 'Railway API token when ready'

jobs:
  deploy:
    name: Deploy to Railway
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Placeholder – Railway deploy not configured
        run: |
          echo "Deploy to Railway will be implemented here."
          echo "Configure RAILWAY_TOKEN secret and uncomment deploy steps in workflow."
